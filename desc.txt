Every day at 9, download https://reykjavik.is/byggingarmal/fundargerdir-byggingarfulltrua, extract all pdf links from content, download each pdf, for each pdf, read the content of the pdf file, use llm to determine the title, date and generate summary of addresses, then store in database, title, content, summary, url and date. make sure to store each url only once. When adding address, search if it exists in meetings notes using llm. 

Every day and when adding address use llm to determine if address is mentioned in any meeting. If is mentioned send message using Discord app with a summary of the content where address is mentioned, also display alert in UI about it with link to pdf

When no meeting exists, download all, insert into db, and load the meetings ui after each is downloaded, dont wait for it to finish